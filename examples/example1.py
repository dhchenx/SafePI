from safepi.World import greet_times

# This function's algorithm is from a .pyd (.so in Linux) file compiled by Cython. The source code cannot be found in the package and is therefore protected.
greet_times(n=5)